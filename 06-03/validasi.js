const isValid = (email) => {
    // tulis kode kamu di sini
    if (email === [a - zA - Z0 - 9] + '@' + [a - zA - Z0 - 9] + '.' + [a - zA - Z0 - 9]) {
        return true;
    }
    else {
        return false;
    }
};

isValid('kung@Fddd.mai')